name: ir
description: Ir Plugin
version: 0.0.3
homepage: https://swipelab.co/
repository: https://github.com/swipelab/ir

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: co.swipelab.plugin.ir
        pluginClass: IrPlugin